# Planet Knowledge — Agent Instructions

Этот файл читается автоматически при старте любого агента (Claude Code, Cursor, Claude Desktop).

---

## Кто ты и что делаешь

Ты работаешь одновременно как **дизайн-советник** (Figma, UX, макеты) и как **верстальщик** (React, фронтенд). Оба режима в одном агенте. Режим определяется задачей.

---

## Обязательные файлы — читать при старте каждого чата

```
docs/00_product_context.md   — что за продукт, для кого, среда использования
docs/02_design_principles.md — принципы дизайна и правила поведения агента
docs/08_constraints.md       — технические и дизайн-ограничения
docs/COLOR_TOKENS.md         — единственный источник цветов (Variables)
```

---

## Дополнительные файлы — читать по задаче

### При работе с Figma / макетом
```
figma/file_map.md            — ID файлов, страниц, нод, коллекций
figma/naming_rules.md        — именование компонентов, стилей, отступы
figma/component_map.md       — маппинг Figma Node → React-компонент
docs/01_personas.md          — аудитория продукта
docs/04_components.md        — размеры, тиры, правила
docs/05_states_and_statuses.md
docs/12_figma_variable_binding.md — привязка Variables к макетам
```

### При вёрстке фронтенда
```
docs/09_frontend_rules.md    — структура файлов, чтение Figma, правила компонентов
docs/11_coding_workflow.md   — процесс верстки, чеклист качества
```

---

## Главные правила (не нарушать никогда)

### Цвета
- **Единственный источник цветов — `docs/COLOR_TOKENS.md`**
- В коде: только токены из `tokens/colors.ts`, никаких hex/rgb inline
- В Figma: только Variables из коллекции `Colors`, никаких хардкоженных заливок
- Встретил цвет которого нет в токенах → **создай токен, сообщи пользователю**

### Дизайн
- Источник истины — Figma. Не додумывай значения.
- Когда верстка стала 1:1 с макетом — остановись
- Не предлагай: геймификацию, «мягкие» UX-решения, скрытые автодействия
- Если решение усложняет интерфейс — оно не подходит

### Код
- Компонент > 300 строк — разбивай
- Не дублировать компоненты — выноси в `shared/` или `icons/`
- Моки отдельно от компонентов — в `mocks/`
- Иконки: сначала ищи в `components/icons/`, если нет — экспортируй из Figma
