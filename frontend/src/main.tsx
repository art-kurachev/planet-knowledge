import React from 'react'
import ReactDOM from 'react-dom/client'
import { SkudReport } from './skud-report'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SkudReport />
  </React.StrictMode>,
)
